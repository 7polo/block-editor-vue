import BlockEditor from './BlockEditor';

export default BlockEditor;

import {STATE, EVENT_TYPES} from './constant';

export const EDITOR_STATE = STATE;
export const EDITOR_EVENT_TYPES = EVENT_TYPES;
